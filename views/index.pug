extends layout

block content
  h1= title

  if !user
    p Welcome! Please 
      a(href='/login') log in
      |  or 
      a(href='/sign-up') sign up
      | .

  else
    p Welcome, #{user.username}.
